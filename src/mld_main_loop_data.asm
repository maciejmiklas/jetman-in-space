;----------------------------------------------------------;
;                    Game Counters                         ;
;----------------------------------------------------------;
	MODULE mld 

counter000FliFLop	BYTE 0						; Changes with every counter run from 1 to 0 and so on.

COUNTER002_MAX		= 2
counter002			BYTE 0
counter002FliFLop	BYTE 0						; Changes with every counter run from 1 to 0 and so on.

COUNTER004_MAX		= 4
counter004			BYTE 0
counter004FliFLop	BYTE 0						; Changes with every counter run from 1 to 0 and so on.

COUNTER006_MAX		= 6
counter006			BYTE 0
counter006FliFLop	BYTE 0						; Changes with every counter run from 1 to 0 and so on.

COUNTER008_MAX		= 8
counter008			BYTE 0
counter008FliFLop	BYTE 0						; Changes with every counter run from 1 to 0 and so on.

COUNTER010_MAX		= 10
counter010			BYTE 0
counter010FliFLop	BYTE 0						; Changes with every counter run from 1 to 0 and so on.

COUNTER020_MAX		= 20
counter020			BYTE 0
counter020FliFLop	BYTE 0						; Changes with every counter run from 1 to 0 and so on.

COUNTER040_MAX		= 40
counter040			BYTE 0

COUNTER080_MAX		= 80
counter080			BYTE 0
;----------------------------------------------------------;
;                       ENDMODULE                          ;
;----------------------------------------------------------;
	ENDMODULE